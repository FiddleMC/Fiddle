--- a/src/main/java/io/papermc/paper/registry/entry/RegistryEntryMeta.java
+++ b/src/main/java/io/papermc/paper/registry/entry/RegistryEntryMeta.java
@@ -113,6 +_,11 @@
         @Override
         public org.bukkit.Registry<A> createApiRegistry(final Registry<M> nmsRegistry) {
             if (this.modificationApiSupport.canAdd()) {
+                // Fiddle start - More data-driven - Registry API - Implementation - Make block and item registries key-aware
+                if (nmsRegistry instanceof org.fiddlemc.fiddle.impl.moredatadriven.minecraft.BlockRegistry || nmsRegistry instanceof org.fiddlemc.fiddle.impl.moredatadriven.minecraft.ItemRegistry) {
+                    return (org.bukkit.Registry<A>) new org.fiddlemc.fiddle.impl.moredatadriven.paper.KeyAwareWritableCraftRegistry((MappedRegistry<M>) nmsRegistry, this);
+                }
+                // Fiddle end - More data-driven - Registry API - Implementation - Make block and item registries key-aware
                 return new WritableCraftRegistry<>((MappedRegistry<M>) nmsRegistry, this);
             } else {
                 return ServerSide.super.createApiRegistry(nmsRegistry);

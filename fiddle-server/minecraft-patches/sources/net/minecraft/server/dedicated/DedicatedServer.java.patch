--- a/net/minecraft/server/dedicated/DedicatedServer.java
+++ b/net/minecraft/server/dedicated/DedicatedServer.java
@@ -1,15 +_,12 @@
 package net.minecraft.server.dedicated;
 
 import com.google.common.collect.ImmutableMap;
-import com.google.common.collect.Lists;
 import com.google.common.collect.ImmutableMap.Builder;
 import com.google.common.net.HostAndPort;
 import com.mojang.datafixers.DataFixer;
 import com.mojang.logging.LogUtils;
 import io.netty.handler.ssl.SslContext;
-import java.io.BufferedReader;
 import java.io.IOException;
-import java.io.InputStreamReader;
 import java.io.Writer;
 import java.net.InetAddress;
 import java.net.Proxy;
@@ -18,7 +_,6 @@
 import java.nio.file.Files;
 import java.nio.file.LinkOption;
 import java.nio.file.Path;
-import java.util.Collections;
 import java.util.List;
 import java.util.Locale;
 import java.util.Map;
@@ -261,6 +_,7 @@
         this.paperConfigurations.initializeGlobalConfiguration(this.registryAccess());
         this.paperConfigurations.initializeWorldDefaultsConfiguration(this.registryAccess());
         // Paper end - initialize global and world-defaults configuration
+        org.fiddlemc.fiddle.impl.configuration.FiddleConfigurations.get(this.options).initializeWorldDefaultsConfiguration(this.registryAccess()); // Fiddle - Configuration - Initialize world-defaults configuration
         this.server.spark.enableEarlyIfRequested(); // Paper - spark
         // Paper start - fix converting txt to json file; convert old users earlier after PlayerList creation but before file load/save
         if (this.convertOldUsers()) {
@@ -275,6 +_,11 @@
         com.destroystokyo.paper.Metrics.PaperMetrics.startMetrics(); // Paper - start metrics
         com.destroystokyo.paper.VersionHistoryManager.INSTANCE.getClass(); // Paper - load version history now
 
+        org.fiddlemc.fiddle.impl.packetmapping.component.translatable.ServerSideTranslationRegistrarImpl.get().fireComposeEvent(); // Fiddle - Server-side translations - Implementation - Fire compose event to register translations
+        org.fiddlemc.fiddle.impl.packetmapping.component.ComponentMappingPipelineImpl.get().fireComposeEvent(); // Fiddle - Component mapping - Implementation - Fire compose event to register mappings
+        org.fiddlemc.fiddle.impl.packetmapping.block.BlockMappingPipelineImpl.get().fireComposeEvent(); // Fiddle - Block mapping - Implementation - Fire compose event to register mappings
+        org.fiddlemc.fiddle.impl.packetmapping.item.ItemMappingPipelineImpl.get().fireComposeEvent(); // Fiddle - Item mapping - Implementation - Fire compose event to register mappings
+
         // this.worldData.setGameType(properties.gameMode.get()); // CraftBukkit - moved to world loading
         LOGGER.info("Default game type: {}", properties.gameMode.get());
         // Paper start - Unix domain socket support
@@ -315,6 +_,11 @@
             if (true) throw new IllegalStateException("Failed to bind to port", var11); // Paper - Propagate failed to bind to port error
             return false;
         }
+
+        // Fiddle start - Material injection - Run on server start, before Bukkit plugins are loaded
+        org.fiddlemc.fiddle.impl.bukkit.enuminjection.material.MaterialEnumNameMappingPipelineImpl.get().fireComposeEvent();
+        org.fiddlemc.fiddle.impl.bukkit.enuminjection.material.MaterialEnumSynchronizer.get().runFromDedicatedServerInit();
+        // Fiddle end - Material injection - Run on server start, before Bukkit plugins are loaded
 
         // CraftBukkit start
         this.server.loadPlugins();
